{% comment %}
  Hero Banner Section
  
  This section creates a hero banner (large promotional area) typically used
  at the top of the homepage or landing pages. It displays a heading, text,
  optional image background, and a call-to-action button. The section supports
  multiple layout variations (centered, left, right), color scheme overrides,
  and spacing presets. The image can have an overlay for better text readability.
{% endcomment %}

{% liquid
  assign layout_variation = section.settings.layout_variation | default: 'centered'
  assign abd_theme = section.settings.abd_theme
  assign custom_bg = section.settings.custom_bg
  assign custom_text = section.settings.custom_text
  assign custom_accent = section.settings.custom_accent
  assign custom_link = section.settings.custom_link
  
  assign section_class = 'section-hero-banner'
  if layout_variation != 'default'
    assign section_class = section_class | append: ' layout-' | append: layout_variation
  endif
  if abd_theme != blank
    assign section_class = section_class | append: ' abd-theme-' | append: abd_theme
  endif
  
  if section.settings.full_width
    assign section_class = section_class | append: ' full-width'
  endif

  assign horizontal_align = section.settings.content_alignment_horizontal | default: 'center'
  assign vertical_align = section.settings.content_alignment_vertical | default: 'center'
  
  assign section_class = section_class | append: ' align-h-' | append: horizontal_align
  assign section_class = section_class | append: ' align-v-' | append: vertical_align
  assign image_loading = section.settings.image_loading | default: 'eager'
%}

<div class="{{ section_class }}"
     style="{% if custom_bg != blank %}background-color: {{ custom_bg }};{% endif %} {% if custom_text != blank %}color: {{ custom_text }};{% endif %}">
  {% if section.settings.image != blank %}
    <div class="section-hero-banner__image-wrapper">
      {% render 'image', 
        image: section.settings.image, 
        alt: section.settings.heading | default: 'Hero banner',
        class: 'section-hero-banner__image',
        loading: image_loading
      %}
      {% if section.settings.overlay_color != blank %}
        <div class="section-hero-banner__overlay" style="background-color: {{ section.settings.overlay_color }}; opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"></div>
      {% endif %}
    </div>
  {% endif %}
  
  {%- if abd_theme == 'retro-internet' -%}<div class="abd-geocities-box">{%- endif -%}
  
  <div class="section-hero-banner__container">
    {%- if section.settings.heading != blank -%}
      <h2 class="section-hero-banner__heading abd-title">
        {{ section.settings.heading }}
      </h2>
    {%- endif -%}

    {%- if section.settings.text != blank -%}
      <div class="section-hero-banner__text abd-content">
        {{ section.settings.text }}
      </div>
    {%- endif %}

    {%- if section.settings.button_text != blank and section.settings.button_link != blank -%}
      <div class="section-hero-banner__button">
        {% render 'button', 
          text: section.settings.button_text, 
          url: section.settings.button_link,
          size: section.settings.button_size | default: 'large'
        %}
      </div>
    {%- endif %}
  </div>
  
  {%- if abd_theme == 'retro-internet' -%}</div>{%- endif -%}
</div>

{% style %}
  .section-hero-banner {
    position: relative;
    padding-top: {{ section.settings.padding_top | default: 60 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 60 }}px;
    min-height: {{ section.settings.min_height | default: 400 }}px;
    display: flex;
    overflow: hidden;
  }

  .section-hero-banner.align-h-left { justify-content: flex-start; }
  .section-hero-banner.align-h-center { justify-content: center; }
  .section-hero-banner.align-h-right { justify-content: flex-end; }

  .section-hero-banner.align-v-top { align-items: flex-start; }
  .section-hero-banner.align-v-center { align-items: center; }
  .section-hero-banner.align-v-bottom { align-items: flex-end; }
  
  .section-hero-banner__image-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }
  
  .section-hero-banner__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: {{ section.settings.image_position | default: 'center' }};
  }
  
  .section-hero-banner__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }
  
  .section-hero-banner__container {
    position: relative;
    z-index: 2;
    max-width: {{ section.settings.content_max_width | default: 800 }}px;
    width: 100%;
    padding: var(--spacing-lg) var(--layout-container-padding);
    text-align: {{ section.settings.content_alignment_horizontal | default: 'center' }};
  }
  
  .section-hero-banner.layout-left .section-hero-banner__container {
    text-align: left;
  }
  
  .section-hero-banner.layout-right .section-hero-banner__container {
    text-align: right;
  }
  
  /* Split Layout */
  .section-hero-banner.layout-split {
    flex-direction: row;
    align-items: stretch;
    min-height: 500px;
  }
  
  .section-hero-banner.layout-split .section-hero-banner__image-wrapper {
    position: relative;
    width: 50%;
    height: auto;
  }
  
  .section-hero-banner.layout-split .section-hero-banner__container {
    width: 50%;
    padding: var(--spacing-xl);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  
  @media (max-width: 768px) {
    .section-hero-banner.layout-split {
      flex-direction: column;
    }
    
    .section-hero-banner.layout-split .section-hero-banner__image-wrapper,
    .section-hero-banner.layout-split .section-hero-banner__container {
      width: 100%;
    }
  }
  
  .section-hero-banner__heading {
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-heading);
    color: var(--color-text);
    margin-bottom: var(--spacing-md);
  }
  
  .section-hero-banner__text {
    font-size: var(--font-size-body);
    line-height: var(--line-height-base);
    color: var(--color-text);
    margin-bottom: var(--spacing-lg);
  }
  
  .section-hero-banner__button {
    margin-top: var(--spacing-md);
  }
  .section-hero-banner.full-width {
    width: 100%;
    max-width: none;
    margin: 0;
  }
{% endstyle %}

{% render 'abd-theme-styles',
  theme: abd_theme,
  custom_bg: custom_bg,
  custom_text: custom_text,
  custom_accent: custom_accent,
  custom_link: custom_link
%}

{% schema %}
{
  "name": "Hero Banner",
  "tag": "section",
  "class": "section-hero-banner",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome to our store"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Shop the latest collections</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero Image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image Position",
      "default": "center",
      "options": [
        { "label": "Top", "value": "top" },
        { "label": "Center", "value": "center" },
        { "label": "Bottom", "value": "bottom" }
      ]
    },
    {
      "type": "select",
      "id": "content_alignment_horizontal",
      "label": "Content Horizontal Alignment",
      "default": "center",
      "options": [
        { "label": "Left", "value": "left" },
        { "label": "Center", "value": "center" },
        { "label": "Right", "value": "right" }
      ]
    },
    {
      "type": "select",
      "id": "content_alignment_vertical",
      "label": "Content Vertical Alignment",
      "default": "center",
      "options": [
        { "label": "Top", "value": "top" },
        { "label": "Middle", "value": "center" },
        { "label": "Bottom", "value": "bottom" }
      ]
    },
    {
      "type": "range",
      "id": "content_max_width",
      "min": 400,
      "max": 1200,
      "step": 50,
      "unit": "px",
      "label": "Content Max Width",
      "default": 800
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "default": 40,
      "min": 0,
      "max": 100,
      "step": 5
    },
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full Width Section",
      "default": false,
      "info": "Extends the banner to the full width of the screen, breaking out of the container."
    },
    {
      "type": "select",
      "id": "layout_variation",
      "label": "Layout Variation",
      "default": "centered",
      "options": [
        { "value": "centered", "label": "Centered" },
        { "value": "split", "label": "Split Layout" },
        { "value": "left", "label": "Left Aligned" },
        { "value": "right", "label": "Right Aligned" }
      ]
    },
    {
      "type": "select",
      "id": "button_size",
      "label": "Button Size",
      "default": "large",
      "options": [
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" }
      ]
    },
    {
      "type": "select",
      "id": "image_loading",
      "label": "Image Loading",
      "default": "eager",
      "info": "Hero images should load immediately for best performance",
      "options": [
        { "value": "eager", "label": "Eager Load (Recommended)" },
        { "value": "lazy", "label": "Lazy Load" }
      ]
    },
    {
      "type": "checkbox",
      "id": "enable_parallax",
      "label": "Enable Parallax Effect",
      "default": false,
      "info": "Adds depth effect on scroll (may impact performance)"
    },
    {
      "type": "header",
      "content": "Theme Style"
    },
    {
      "type": "select",
      "id": "abd_theme",
      "label": "ABD Theme Style",
      "default": "",
      "options": [
        { "value": "", "label": "None (Theme Default)" },
        { "value": "cable-news", "label": "Cable News" },
        { "value": "california-dreaming", "label": "California Dreaming" },
        { "value": "california-mission", "label": "California Mission" },
        { "value": "cubism", "label": "Cubism" },
        { "value": "disco", "label": "Disco" },
        { "value": "france", "label": "France" },
        { "value": "free-love", "label": "Free Love" },
        { "value": "highway-street-photo", "label": "Highway Street Photo" },
        { "value": "impressionist", "label": "Impressionist" },
        { "value": "italy", "label": "Italy" },
        { "value": "men's-magazine", "label": "Men'S Magazine" },
        { "value": "military-theme", "label": "Military Theme" },
        { "value": "millennial-myspace", "label": "Millennial Myspace" },
        { "value": "national-parks-poster", "label": "National Parks Poster" },
        { "value": "newspaper", "label": "Newspaper" },
        { "value": "nineties-graphic-design", "label": "Nineties Graphic Design" },
        { "value": "renaissance", "label": "Renaissance" },
        { "value": "retro-diner", "label": "Retro Diner" },
        { "value": "retro-internet", "label": "Retro Internet" },
        { "value": "retro-tech-polaroid", "label": "Retro Tech Polaroid" },
        { "value": "stars-and-stripes", "label": "Stars And Stripes" },
        { "value": "stpatricks", "label": "Stpatricks" },
        { "value": "synthwave", "label": "Synthwave" },
        { "value": "travel-nyc-liberty", "label": "Travel Nyc Liberty" },
        { "value": "valentines-her", "label": "Valentines Her" },
        { "value": "wayfinding-receipt", "label": "Wayfinding Receipt" },
        { "value": "zine", "label": "Zine" }
      ]
    },
    {
      "type": "header",
      "content": "Custom Colors (Optional)"
    },
    {
      "type": "paragraph",
      "content": "Overrides the selected theme's default colors."
    },
    {
      "type": "color",
      "id": "custom_bg",
      "label": "Custom Background"
    },
    {
      "type": "color",
      "id": "custom_text",
      "label": "Custom Text"
    },
    {
      "type": "color",
      "id": "custom_accent",
      "label": "Custom Accent"
    },
    {
      "type": "color",
      "id": "custom_link",
      "label": "Custom Link"
    },
    {
      "type": "header",
      "content": "Section Spacing"
    },
    {
      "type": "range",
      "id": "min_height",
      "min": 200,
      "max": 800,
      "step": 50,
      "unit": "px",
      "label": "Minimum Height",
      "default": 400
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 10,
      "unit": "px",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 10,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "Hero Banner",
      "settings": {
        "heading": "Welcome to our store",
        "text": "<p>Shop the latest collections</p>",
        "button_text": "Shop Now",
        "image_position": "center",
        "overlay_color": "#000000",
        "overlay_opacity": 40
      }
    }
  ]
}
{% endschema %}